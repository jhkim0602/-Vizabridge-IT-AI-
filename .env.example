# Vizabridge currently runs the LLM stages through Claude Code skills
# (no external API key required). This file is kept so future stages
# (embeddings, RAG retrieval, etc.) have a place to declare keys.
